/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "picsum.photos",
      "placehold.co",
      "fastly.picsum.photos",
      "cloud.appwrite.io",
    ],
  },
};

module.exports = nextConfig;
